## 概要
- 課題: クリアタイムだけでは、ターゲット外クリックの正確さを含めてプレイ結果を比較できない。
- 目的: 速さと正確さを整数ポイントへ換算し、ひと目で好成績を判断できるスコア方式を追加する。
- 完了条件: `Math.round(10000 / Math.max(クリアタイム（秒） + ミス回数 × 2, 0.01))`でスコアを計算し、`ja-JP`の3桁区切りを使った整数`pt`として表示する。高いほど好成績と明示し、ゲーム外のクリックをミスへ含めず、既存の難易度・一時停止・Finish表示を維持する。

## 適用した役割
### Portfolio Planner
- 入力: タイムとミスを組み合わせたスコア方式の要望、比較した5案、既存ゲームフロー。
- 実施内容: ユーザー選択の案4を最終仕様とし、調整後タイムの逆数へ10,000を掛けたポイント方式へ整理した。ランキング、ベスト保存、ランク判定は対象外とした。
- 成果物: `10,000 ÷（タイム + ミス1回につき2秒）`を四捨五入した整数ポイントと、高いほど良いという評価ルール。
- 検証結果: タイム短縮とミス削減のどちらも常に得点上昇へつながることを確認した。
- 未解決事項: 得点分布とミス2秒の重みは実プレイデータがないため未評価。
- 次工程への引き継ぎ: 固定ルールで実装し、重みの再調整は別課題とする。

### Portfolio UI Designer
- 入力: `DESIGN.md`、既存のStatusカード、viewport中央固定のFinish表示。
- 実施内容: Statusエリアへ既存カード形式のスコア表示を追加し、Finish表示をポイント・タイム・ミス補正・ミス回数がまとまった結果カードへ拡張した。
- 成果物: 開始前は「—」と確定条件・評価方向、クリア時は整数ポイントと計算内訳を示す表示。既存の配色、角丸、タイポグラフィを維持した。
- 検証結果: Chromiumで結果カードのviewport中央配置とモバイル幅への収まりを確認した。320×568、375×667、390×844、640×800でFinishカードと「もう一度」ボタンが重ならないことも確認した。
- 未解決事項: なし。
- 次工程への引き継ぎ: なし。

### Portfolio Copywriter
- 入力: 案4の計算式、整数ポイント、高いほど良いという評価方向、既存の画面文言。
- 実施内容: ヘッダーを「速く正確にクリアする」目的と計算式が短く伝わる文言へ変更した。未確定表示、Finish注記、live regionもポイント方式と高得点評価へ統一した。
- 成果物: 「速く正確にクリアして、高得点を目指そう。スコア = 10,000 ÷（タイム + ミス1回につき2秒）です。」を基準とする表示文言。
- 検証結果: すべての画面文言を整数ポイント単位と高得点評価へ統一した。
- 未解決事項: なし。
- 次工程への引き継ぎ: 実表示で改行と読みやすさを確認する。

### Portfolio Frontend Engineer
- 入力: 案4の確定式、整数ポイント表示、状態別表示、既存のスコア実装とアクセシビリティ要件。
- 実施内容: `SCORE_BASE = 10000`と`MISS_PENALTY_SECONDS = 2`を定数化し、純粋関数`calculateScore`へ計算を分離した。整数ポイントを`Intl.NumberFormat("ja-JP")`で整形し、視覚表示は`pt`、live regionは「ポイント」と用途別に表記した。開始・リスタート・やめる時のリセット、プレイ中の難易度固定、arena背景のみのミス判定、クリア後のフォーカス移動は維持した。差し戻し対応として640px以下ではフォーカス後のStartボタンを固定ヘッダー下へスクロールし、Finishカードとの重なりを避ける処理を追加した。
- 成果物: 案4のポイント計算、3桁区切り表示、用途別の単位表記、タイム・ミス補正・ミス回数の内訳、モバイルで次操作を見失わないフォーカス配置。
- 検証結果: `node --check`合格、`calculateScore`の4ケースと`ja-JP`整形の確認合格、HTMLの20 ID重複なし・JavaScriptからの19参照解決、CSS波括弧整合、`git diff --check`合格を確認した。
- 未解決事項: なし。
- 次工程への引き継ぎ: Performance & Accessibility TesterとReviewerが案4への要件適合と既存非回帰を確認する。

### Portfolio Performance & Accessibility Tester
- 入力: 案4へ変更後のHTML、CSS、JavaScript、既存の非回帰条件。
- 実施内容: Playwright確認で、320×568のクリア時にFinishカードがフォーカスされたStartボタンを覆う中問題1件と、live regionが英字の`pt`を読み上げる軽微問題1件を指摘した。
- 成果物: 640px以下だけ`scrollIntoView`を行い、`scroll-margin-top: 60px`で固定ヘッダーを避ける修正。視覚用`pt`と読み上げ用「ポイント」を分離する修正。
- 検証結果: Playwright + Chromiumで、320×568、375×667、390×844、640×800におけるFinishカードとfocused Startボタンの矩形非重複、固定ヘッダー回避、viewport内表示を確認した。live regionが「ポイント」を含むこと、Finish中央固定、Easy/Hard角丸、Hard全域の罫線と背景移動、状態リセットも合格し、console error・page error・failed requestは0件だった。
- 未解決事項: 実スクリーンリーダーによる音声確認は未実施。DOM上の読み上げ文言とlive region更新は確認済み。
- 次工程への引き継ぎ: Reviewerが最終差分と実動結果を確認する。

### Portfolio Reviewer
- 入力: 案4へ更新した実装差分、静的検証、今後のPlaywright検証結果、本Issue記録。
- 実施内容: 案4の実装と初回Playwright結果を確認し、モバイルのフォーカス可視性を中1件、live regionの単位表記を軽微1件として差し戻した。修正後は最新4ファイル、Issue記録、静的検証、実動再検証を再レビューした。
- 成果物: モバイル限定スクロールと読み上げ専用ポイント表記による修正差分、およびReady判定。
- 検証結果: 修正後のPlaywright再検証は全項目合格。320×568を含む4種類のviewportでフォーカス可視性を確認し、live regionの単位も「ポイント」へ更新された。要件適合、既存非回帰、無関係変更の不在を確認し、重大・中・軽微の未解決指摘は0件。
- 未解決事項: なし。
- 次工程への引き継ぎ: 最終静的確認後、コミット対象を限定して記録する。

## 主要な判断
- 判断: スコアは調整後タイムを分母とする整数ポイント方式にする。
- 理由: 速さと正確さを一つの値へまとめ、高得点を目指す一般的なゲーム表現に合わせるため。
- 判断: 分母の下限を0.01秒とし、最後に`Math.round`で整数化する。
- 理由: ゼロ除算を防ぎ、計算途中の精度を保ったまま安定した整数ポイントを表示するため。
- 判断: ミスは`arena`背景のクリックイベントだけで加算する。
- 理由: 操作ボタン、難易度、ナビゲーション、スクロールバーをゲーム上のミスから確実に除外するため。
- 判断: 視覚結果カードとlive regionを分離する。
- 理由: 詳細なカードを視覚表示しながら、支援技術にはまとまった結果を一度だけ通知するため。
- 判断: クリア時は「もう一度」ボタンへフォーカスを移す。
- 理由: キーボード操作中のターゲットが非表示になっても、次の操作位置を見失わないようにするため。
- 判断: 640px以下だけフォーカス後のStartボタンをページ上部へスクロールし、60pxのscroll marginを持たせる。
- 理由: 固定ヘッダーを避けながら、viewport中央固定のFinishカードと次操作ボタンを320×568でも同時に視認できるようにするため。
- 判断: 視覚表示は`pt`、live regionは「ポイント」とする。
- 理由: コンパクトな視覚表示を維持しつつ、支援技術で単位を明確に読み上げるため。

## 最終結果
- 解決したこと: 案4の整数ポイント方式へスコア計算と全表示文言を更新した。難易度固定、arenaミス判定、状態リセット、Finish中央固定、読み上げ通知は維持した。
- 変更ファイル: `games/trackball-controll-practice.html`、`css/trackball-controll-practice.css`、`scripts/trackball-controll-practice.js`、`Issues/Issue_013.md`。
- 検証結果: 中1件・軽微1件の差し戻しに対応。静的検証とPlaywright + Chromiumによる実動再検証に合格し、320×568を含む4 viewportでFinishカードとfocused Startボタンが重ならないことを確認した。console error・page error・failed requestは0件。
- 未解決事項: 実スクリーンリーダーによる音声確認は未実施。重大な未解決事項は0件。
- 次アクション: 実プレイデータが集まった段階で、必要に応じてミス補正2秒の重みを別課題で調整する。
