# Portfolio site agents

このリポジトリでは、Codexを主担当エージェントとして利用する。
Codexは依頼内容を分類し、必要な専門役割だけをサブエージェントへ委任しながら、実装・検証・記録を統括する。

## 1. 適用範囲と基本原則

- このファイルはリポジトリ全体に適用する。
- ユーザーの依頼範囲を超える変更を行わない。
- 既存構成と既存のユーザー変更を尊重し、目的を満たす最小差分を優先する。
- 不要なライブラリ追加、過度な抽象化、大規模な再設計を避ける。
- 調査・説明・レビューだけの依頼では、明示されていない実装や外部への変更を行わない。
- 主担当Codexが最終判断、成果物の整合確認、ユーザーへの完了報告を担当する。
- 最終完了条件は「重大な未解決事項が0件」である。

## 2. タスク分類と役割選択

全案件で7役すべてを機械的に起動しない。主担当Codexがタスクに必要な役割だけを選ぶ。

| タスク | 基本の担当役割 |
| --- | --- |
| UIの新規作成・大幅変更 | Planner → UI Designer → Frontend Engineer → Reviewer → Performance & Accessibility Tester |
| 小さなUI修正 | UI DesignerまたはFrontend Engineer → Reviewer → 必要範囲のTester |
| ロジック・不具合修正 | Frontend Engineer → Reviewer。体験やA11yへ影響する場合はTesterも追加 |
| コピー・文章のみ | Copywriter → Reviewer |
| SEO・計測 | SEO & Analytics Specialist → Frontend Engineer → Reviewer |
| 調査・説明・レビューのみ | 最も適した1役。必要な場合だけ追加 |
| 設定・ドキュメント | Frontend Engineer相当。必要な場合はReviewerを追加 |

- 文言を変更する場合だけCopywriterを追加する。
- title、description、OGP、構造化データ、計測、公開導線へ影響する場合だけSEO & Analytics Specialistを追加する。
- 小規模で要件が明確な修正では、PlannerやUI Designerを省略できる。
- ユーザーが役割や進行順を指定した場合は、その指示を優先する。

## 3. サブエージェントへの委任

- 専門役割の作業は、原則として該当するサブエージェントへ委任する。
- 委任内容は、具体的で範囲の明確なタスクにする。
- 依存関係のある工程は順番にバトンタッチする。
- 独立して実行できる調査、レビュー、テストだけを並列化できる。
- 同じファイルを複数のサブエージェントが同時編集しないよう、主担当Codexが担当範囲を分ける。
- 各役割は、前工程から受け取った「入力」を確認し、完了時に次を返す。
  - 実施内容
  - 成果物
  - 検証結果
  - 未解決事項
  - 次工程への引き継ぎ
- サブエージェントは担当範囲を超える変更を行わず、追加判断が必要な場合は主担当Codexへ返す。
- ReviewerまたはTesterが重大問題を発見した場合は、Frontend Engineerへ差し戻し、修正後に再レビューする。
- サブエージェントを利用できない場合は、主担当Codexが同じ役割基準で作業し、その旨を報告する。

## 4. GitブランチとPRワークフロー

### 適用条件

- ファイル変更を伴う課題では、ユーザーが明示的に不要としない限り、Codexが課題専用の作業ブランチを作成する。
- 原則として、ブランチ作成後に実装、検証、Issue記録、コミット、push、PR作成、レビュー対応まで行う。
- 調査、説明、状況確認、レビューだけでファイルを変更しない課題では、ブランチやPRを作成しない。
- ユーザーが「ブランチ不要」「コミット不要」「PR不要」「ローカル変更のみ」と指定した場合は、その範囲を省略する。
- PRのマージと作業ブランチの削除は自動で行わず、ユーザーから明示的な指示がある場合だけ実施する。

### Git操作の承認ポリシー

- 安全で通常のGit/PR工程は、初回にコマンド単位の永続的な許可を求め、承認済みの範囲では以後の確認を繰り返さず自動実行する。
- 自動実行の対象は、状態・差分・履歴の確認、fetch、課題用ブランチ作成、対象を限定したstage、commit、通常のpush、PRの表示・差分・checks確認、レビューコメント、レビュー指摘への通常対応とする。
- PRの新規作成は、外部公開される単位をユーザーが確認できるよう、毎回明示的な許可を得てから実行する。
- PRのマージ、ローカルでのmerge、remote/localブランチの削除、force-push、rebase・commit amend・resetなどの履歴書き換え、ユーザー変更のstash・破棄は、自動承認の対象外とし、毎回明示的な許可を得る。
- 実行環境が承認済み操作にも確認を要求する場合は、対象を限定した永続的なコマンド許可を求める。このファイルの記載だけで実行環境の権限確認を回避しない。
- 広すぎるコマンド許可を求めず、`git commit`、`git push`、`gh pr view`のように工程ごとの安全な範囲へ限定する。

### 作業開始前

- 変更前に現在のブランチ、working tree、未追跡ファイル、remote、デフォルトブランチ、baseとの差分を確認する。
- 同じ課題用としてCodexが作成済みのブランチがある場合は、新規作成せず継続利用する。
- ユーザー管理のfeature branchへ、別課題の変更を無断で追加しない。
- Codexが現在の課題で作成した未コミット変更は、課題用ブランチへ保持したまま移動できる。
- ユーザーの変更や別課題の変更を、stash、reset、checkoutなどで無断破棄・退避しない。
- 無関係なdirty worktreeは、可能なら別worktreeで分離する。同じファイルで競合する、または所有者・目的を判断できない場合はユーザーへ確認する。
- baseはremoteのデフォルトブランチを優先し、特定できない場合は`main`、`master`、ユーザー指定の順に確認する。
- 可能な場合はremoteをfetchし、最新の`origin/<base>`から分岐する。baseブランチ自体へ直接コミットしない。

### ブランチ命名

- 原則として`codex/<issue-number>-<short-slug>`形式を使う。
- 例: `codex/009-add-pr-workflow`
- Issue番号をまだ確定できない場合は、`codex/<type>-<short-slug>`形式を使える。
- `type`は`feat`、`fix`、`docs`、`refactor`、`chore`、`test`などから変更目的に合わせて選ぶ。
- 小文字英数字とハイフンを使い、短く目的が分かる名前にする。

### コミット

- テストとレビューに耐えられる論理単位でコミットし、無関係な変更を含めない。
- 対象ファイルを明示してstageし、作業ツリー全体を無条件にstageしない。
- コミット前にstaged diffを確認する。
- Issue記録が必要な課題では、対応する`Issues/Issue_XXX.md`も同じブランチとコミットへ含める。
- コミットメッセージは変更目的が分かる簡潔な形式にする。
- hooksや検証を`--no-verify`で回避しない。
- rebase、merge、履歴変更が必要な場合は影響を確認し、ユーザー作成の履歴を書き換えない。
- force-pushは原則禁止し、必要な場合は理由と対象を示して承認を得る。

### PR作成前レビュー

- 実装と検証の完了後、Portfolio Reviewerのサブエージェントへbaseとの差分レビューを委任する。
- Reviewerは要件適合、テスト結果、Issue記録、無関係な変更の混入を確認する。
- UI変更では、必要に応じてPerformance & Accessibility Testerの結果も確認する。
- 重大問題があれば担当役割へ差し戻し、修正・再検証・再レビューを行う。
- 重大な未解決事項が0件になるまでReady PRを作成しない。外部確認のため共有が必要な場合はDraft PRを使用できる。

### pushとPR作成

- 事前レビュー合格後、作業ブランチをremoteへpushしてPRを作成する。
- PRのbaseとheadを明示し、意図した組み合わせになっていることを確認する。
- PRタイトルは変更目的を簡潔に表す。
- PR本文には最低限、概要、主な変更、検証結果、ブラウザ確認、事前レビュー結果、対応Issue、未解決事項を記載する。
- PR作成後にURL、base、head、DraftまたはReadyの状態を確認する。

### PR作成後のレビュー

- GitHub上のPR差分を改めて確認し、ローカルとの差異や意図しないファイルがないかレビューする。
- CIやrequired checksがある場合は完了まで確認し、失敗時は今回の変更による問題か既存問題かを区別する。
- GitHub上にレビュー指摘がある場合は内容を確認し、依頼範囲内の妥当な指摘を修正、再テスト、commit、pushする。
- 指摘対応後はReviewerへ再レビューを委任し、対応内容をPRへ記録する。
- 要件変更、範囲拡大、ユーザー判断が必要な指摘は勝手に実装せず、ユーザーへ確認する。
- Codex自身による自己承認は、技術的に可能な場合でも正式な外部承認として扱わない。サブエージェントのレビュー結果をPR本文またはコメントへ記録し、外部承認が必要な場合は待機中であることを明示する。
- 存在するCI・required checksの合格、またはCIがないことの確認、重大指摘0件、対応可能なレビュー指摘の解消をPR工程の完了条件とする。

### 認証・権限で続行できない場合

- 利用可能なGitHub接続またはCLIを確認し、認証情報そのものを探索・表示しない。
- 認証不足、remote不在、push権限不足、GitHub API障害などの場合は、ローカルで可能な実装、検証、レビュー、コミットまで完了する。
- 完了報告では、停止した工程、原因、作成済みブランチとコミット、ユーザーに必要な操作を明記する。
- pushまたはPR作成が未完了の場合は、PR工程まで完全完了したと報告しない。

### 標準工程

```text
Git状態確認
→ dirty worktreeの保護
→ base取得・作業ブランチ作成
→ 必要な役割へ委任
→ 実装
→ テスト・ブラウザ確認
→ Issue記録
→ Reviewer事前レビュー
→ 指摘修正・再レビュー
→ scoped staging・commit
→ push・PR作成
→ GitHub上の差分レビュー・CI確認
→ PR指摘対応
→ 最終報告
```

## 5. UI作業の参照ルール

UIの判断基準は、次の優先順位とする。

1. ユーザーの明示要件
2. 既存UIと過去の合意事項
3. `DESIGN.md`
4. shadcn Skill
5. 一般的なUI/UX慣習

### DESIGN.md

- UIの設計・実装・レビューを行う役割は、作業前に`DESIGN.md`を確認する。
- 色、タイポグラフィ、余白、角丸、コンポーネント表現、レスポンシブ方針は原則として`DESIGN.md`に合わせる。
- 既存UIやユーザー要件と競合する場合は、優先順位に従い、逸脱理由を完了報告またはIssueへ記録する。
- `DESIGN.md`は参照基準であり、既存UIへ一括適用する指示ではない。依頼対象外の画面まで変更しない。

### shadcn Skill

- shadcn/ui、registry、`components.json`、プリセット、コンポーネントの追加・修正に関係する場合は、作業前に`.agents/skills/shadcn/SKILL.md`を読む。
- Skillから参照を求められたルールや資料は、対象作業に必要なものだけ追加で確認する。
- shadcn Skillは、コンポーネント選択、構成、CLI、アクセシビリティ、実装パターンの基準として使う。
- `DESIGN.md`とshadcnの標準スタイルが競合する場合は、見た目は`DESIGN.md`を優先し、shadcnは構造と実装パターンへ利用する。
- 現在の静的HTML/CSS構成を優先する。`components.json`がない状態で、shadcn/ui、React、Tailwind、依存パッケージを自動導入しない。
- shadcn/ui本体の初期化やフレームワーク移行が必要な場合は、影響範囲を確認し、ユーザーの依頼または承認を得て別途実施する。

## 6. 実装と検証

### 共通確認

- 変更前後のGit差分を確認し、依頼と無関係な変更を混ぜない。
- HTML、CSS、JavaScript、JSONなど、変更対象に合った構文確認やテストを行う。
- 既存のテストやビルド手順がある場合は、変更範囲に応じて実行する。
- テスト失敗時は、今回の変更による失敗か既存の失敗かを区別する。
- 既存のユーザー変更を上書き、削除、巻き戻ししない。

### ブラウザ実動確認

- HTML、CSS、クライアントJavaScriptなど、表示や操作を変更した場合は実ブラウザで確認する。
- 対象要件について、確認したviewport、操作、期待結果、実結果、合否を記録する。
- 変更に応じて、モバイル表示、キーボード操作、フォーカス、主要な状態変化、既存合意事項の非回帰を確認する。
- 表示コードを変更したにもかかわらずブラウザを利用できない場合は、静的解析や構文確認などの代替検証を行い、理由と未確認範囲を明記する。
- 表示コードのブラウザ確認が未実施の場合は、完全合格や完全完了と報告しない。
- ドキュメント、エージェント設定、配信対象外の設定だけを変更した場合は、ブラウザ確認を省略できる。

## 7. Issue記録

### 作成条件

- ファイル変更を伴う課題では、原則として完了時にIssue記録を作成する。
- 質問、調査、状況確認、レビューのみでファイルを変更しない場合は作成しない。
- ユーザーから別途指示がある場合は、その指示に従って作成要否を判断する。

### 保存先と命名

- 保存先: `Issues/`
- 形式: Markdown
- ファイル名: `Issue_001.md`形式の3桁ゼロ埋め連番
- 採番: `Issue_Template.md`を除く、正規形式の既存最大番号 + 1
- `Issue001.md`、`Issue_01.md`、`Issue-001.md`などの別形式を新規作成しない。
- ファイルをリネームした場合は、リポジトリ内の参照も検索して更新する。

### 記載内容

実際に担当した役割だけを記録し、未使用の役割欄を埋めない。

```md
## 概要
- 課題:
- 目的:
- 完了条件:

## 適用した役割
### Role name
- 入力:
- 実施内容:
- 成果物:
- 検証結果:
- 未解決事項:
- 次工程への引き継ぎ:

## 主要な判断
- 判断:
- 理由:

## 最終結果
- 解決したこと:
- 変更ファイル:
- 検証結果:
- 未解決事項:
- 次アクション:
```

## 8. 専門役割

### Portfolio Planner

- 責務: 目的、対象ユーザー、情報設計、範囲、優先順位、タスク分解を整理する。
- 起動条件: 新規ページ、大幅変更、要件が曖昧な課題、複数セクションへ影響する課題。
- 成果物: 結論、目的とターゲット、推奨構成、優先順位、次のアクション。
- 制約: UI装飾やコードを主役にせず、ページ・セクション単位まで具体化する。

### Portfolio UI Designer

- 責務: レイアウト、視線誘導、配色、タイポグラフィ、余白、CTA、モバイルUIを設計する。
- 起動条件: 見た目、導線、レスポンシブ、インタラクションを変更する課題。
- 成果物: デザイン上の課題、改善方針、レイアウト、モバイル注意点、実装メモ。
- 制約: 過剰な装飾を避け、読みやすさ、信頼感、コントラストを優先する。

### Portfolio Frontend Engineer

- 責務: HTML/CSS/JavaScript、コンポーネント、レスポンシブ、軽量なアニメーションを実装する。
- 起動条件: コード、設定、ドキュメントを変更する課題。
- 成果物: 実装方針、変更箇所、検証結果、注意点、改善候補。
- 制約: 既存構成に合わせた最小変更と可読性を優先し、根拠のない依存追加をしない。

### Portfolio Copywriter

- 責務: 自己紹介、実績、見出し、要約、CTA、英文化を短く具体的に整える。
- 起動条件: ユーザー向け文言を追加・変更する課題。
- 成果物: 必要に応じて短文版、標準版、カジュアル版、英語版。
- 制約: 誇張や中身のない表現を避け、採用担当が必要とする具体性を優先する。

### Portfolio Reviewer

- 責務: UI/UX、実装品質、初期SEO/A11y、要件適合、非回帰を重大度別にレビューする。
- 起動条件: 実装後の品質確認、公開前レビュー、影響範囲が広い変更。
- 成果物: 総評、重大・中・軽微の問題、今すぐ直すべき項目、差し戻し判定。
- 制約: 実動未確認の項目を合格にせず、指摘には再現方法と改善方向を付ける。

### Portfolio SEO & Analytics Specialist

- 責務: title、description、見出し、内部リンク、OGP、構造化データ、計測イベントを設計する。
- 起動条件: SEO、SNS共有、計測、問い合わせや作品閲覧の導線へ影響する課題。
- 成果物: 現状の問題、最小で効く改善、実装メモ、計測項目と確認方法。
- 制約: 小規模サイトへ過剰な施策や多数の計測タグを持ち込まない。

### Portfolio Performance & Accessibility Tester

- 責務: 表示速度、画像、alt/aria、見出し、キーボード操作、フォーカス、コントラストを検証する。
- 起動条件: UI変更、公開前確認、速度やA11yに影響する変更。
- 成果物: 重大度別の問題、再現手順、テスト結果、最優先の改善項目、最終判定。
- 制約: 計測値だけでなく実際の体験を重視し、必要以上の大改修を要求しない。

## 9. プロジェクト固有の非回帰チェック

以下は該当するゲームまたは機能を変更した場合だけ確認する。

- Easy/Hard切替で角丸や形状が失われず、モード間で一貫している。
- Hardモードの背景移動が視認できる。
- Hardモードでスクロール先まで罫線と背景が連続している。
- クリア時のFinish表示が画面中央に固定され、スクロールで移動または消失しない。
- 新規要件への対応によって、過去の合意済み修正が回帰していない。

上記のいずれかが未達の場合は重大問題としてFrontend Engineerへ差し戻す。

## 10. 完了報告

主担当Codexは、最終報告に次を簡潔に含める。

- 解決したこと
- 主な変更ファイル
- 実行した検証と合否
- 作業ブランチ、コミット、PR URL、PRレビューとCIの状態
- 未解決事項
- 必要な次アクション

ブラウザ確認を行った場合は実動結果を明記する。Issueを作成した場合は該当ファイルを案内する。
